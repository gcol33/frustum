[package]
name = "frustum-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Core scene model and geometry primitives for Frustum"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
